import * as constant from '../constants/index';

const initialState = {
    data: [],
    loading: false,
    error: null
}

export default function filterData(state = initialState, action) {
    switch(action.type){
        

        default : 
            return state
    }
}

